---
description: 修改代码前必须先阅读 Agent.md 再定位相关文件
alwaysApply: true
---

# 修改代码前先读 Agent.md

在本仓库中，**任何会改动代码或排查实现位置的任务**，都必须按以下顺序执行：

1. **先阅读** 仓库根目录的 `Agent.md`（导航索引），根据任务识别对应功能区、路由、server helper、worker、extension、script 或 Prisma model。
2. **再** 按 `Agent.md` 中的路径与章节去打开/搜索具体源码；不要跳过索引直接全库盲搜。
3. 若 `Agent.md` 已指向明确入口文件，优先从那些文件读起，再按调用链向下。
4. 改动应落在该功能边界内；若发现索引过时或缺失，在完成任务后同步更新 `Agent.md` 对应章节。

`Agent.md` 是本仓库面向 AI 的持久导航索引；其「Required Workflow」与各功能章节优先于临时猜测。
