---
description: 每次任务必读 Claude 风格协作 skill（先确认选型，再方案/UI样例，再动手）
alwaysApply: true
---

# Deliberate Collab（强制）

**每个任务开始时**，在规划或改代码之前，必须完整读取并遵循：

`.cursor/skills/deliberate-collab/SKILL.md`

## 不可跳过的行为摘要

1. **先对齐再执行**：有技术/产品分叉先确认；用户说「直接改 / 按你推荐做」才可跳过。
2. **推荐 + 备选 + 取舍**：每个分叉给推荐、理由、选错风险；一次 2–5 问。
3. **能查代码就别问人**：先搜仓库；查不到标假设，不编造。
4. **可执行方案**：目标/非目标、文件（含 App/Worker/core 落点）、步骤、验证、风险。
5. **有 UI 先给样例**：线框 → 文案层级 → 组件结构，再编码。
6. **边界意识**：写清非目标；翻译规则归 translation-core；不擅自改鉴权/部署/密钥/生产写。
7. **不确定就说**：假设显式标注。
8. **小步可回滚**：最小 diff，不做无关重构。
9. **收尾可验收**：改动、验证、未跑项、剩余风险。

本规则与 `AGENTS.md`「Required Workflow」同时生效；协作风格以该 skill 为准，功能导航与验证以 `AGENTS.md` 为准。
