---
description: Prefer Polaris for dropdowns; never break Ant Select with CSS overrides
alwaysApply: true
---

# Dropdown / Select 约定

Shopify 嵌入 App 里，下拉框优先用 Polaris，避免 Ant Design Select + 自定义 CSS 拆布局。

## 选型

- **单选**：`@shopify/polaris` 的 `Select`
- **多选**：内联 chip / `ChoiceList` / `Combobox` + `allowMultiple`
- **Ant `Select`**：仅限表格密集筛选、历史 manage 页等已有路径；**不要**在 `app/routes/app.translate-v4/**` 新建 Ant Select

## 禁止

- 用 `!important` 覆盖 `.ant-select-selection-item`、`.ant-select-selector`、`.ant-select-selection-overflow` 等 Ant 内部结构（会把单选拆成「箭头漂一边、选项掉页底」）
- 在折叠面板 / `overflow: hidden` 容器里依赖 Ant Select 默认 portal 却同时改其内部布局

## 改 UI 时

1. 新建下拉先查 Polaris 是否够用
2. 若必须动 Ant Select 样式，只改外层 wrapper，不要改 selection-item 内部
3. 改完在嵌入预览里点开下拉，确认菜单贴着触发器、选中值完整显示
